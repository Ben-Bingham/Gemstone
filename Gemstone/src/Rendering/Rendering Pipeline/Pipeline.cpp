#include "pch.h"
#include "Pipeline.h"

namespace Gem {
	// void Pipeline::AddPass(const Ptr<IPass>& pass) {
	// 	m_Passes.push_back(pass);
	// }
	//
	// void Pipeline::Process() const {
	// 	int i{ 0 };
	// 	void* lastOutput{ nullptr };
	// 	for (const auto& pass : m_Passes) {
	// 		if (i == 0) {
	// 			lastOutput = pass->IProcess(nullptr);
	// 		}
	// 		else {
	// 			lastOutput = pass->IProcess(lastOutput);
	// 		}
	// 		i++;
	// 	}
	// }

}